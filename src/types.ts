export type StepType = { stepNum: number; stepName: string };

export type PlanType = {
  name: string;
  monthlyPrice: number;
  yearlyPrice: number;
  img: string;
};

export type AddonType = {
  name: string;
  monthlyPrice: number;
  yearlyPrice: number;
  description: string;
};
